<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Attendance Report</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Inter', sans-serif;
        }

        /* Simple transition for the accordion content */
        .accordion-content {
            max-height: 0;
            overflow: hidden;
            transition: max-height 0.3s ease-out;
        }

        .accordion-content.open {
            max-height: 1000px;
            /* Set to a large enough value */
            transition: max-height 0.5s ease-in;
        }
    </style>
</head>

<body class="bg-slate-100 text-slate-800">

    <div class="flex h-screen">
        <aside class="w-64 bg-white shadow-md flex-shrink-0">
            <div class="p-6 text-2xl font-bold text-slate-900 border-b">
                Atten<span class="text-indigo-600">Sys</span>
            </div>
            <nav class="p-4">
                <a href="/dashboard/teacher"
                    class="flex items-center p-3 rounded-lg hover:bg-indigo-100 text-slate-700 font-semibold">
                    <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6 mr-3" fill="none" viewBox="0 0 24 24"
                        stroke="currentColor" stroke-width="2">
                        <path stroke-linecap="round" stroke-linejoin="round"
                            d="M4 6a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2H6a2 2 0 01-2-2V6zM14 6a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2h-2a2 2 0 01-2-2V6zM4 16a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2H6a2 2 0 01-2-2v-2zM14 16a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2h-2a2 2 0 01-2-2v-2z" />
                    </svg>
                    Dashboard
                </a>
            </nav>
        </aside>

        <div class="flex-1 flex flex-col overflow-hidden">
            <header class="bg-white border-b p-4 flex justify-between items-center">
                <h1 class="text-xl font-semibold">Attendance Report: <%= subject.name %>
                </h1>
                <a href="/logout"
                    class="flex items-center space-x-2 rounded-lg bg-slate-100 px-4 py-2 text-sm font-semibold text-slate-700 hover:bg-slate-200 transition-colors">
                    <span>Logout</span>
                    <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24"
                        stroke="currentColor" stroke-width="2">
                        <path stroke-linecap="round" stroke-linejoin="round"
                            d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1" />
                    </svg>
                </a>
            </header>

            <main class="flex-1 overflow-y-auto p-6">
                <div class="mb-6">
                    <a href="/dashboard/teacher" class="text-indigo-600 hover:text-indigo-800 font-medium">
                        &larr; Back to Dashboard
                    </a>
                </div>

                <div class="bg-white p-6 rounded-lg shadow-sm max-w-4xl mx-auto">
                    <h2 class="text-2xl font-bold text-slate-900 mb-6 border-b pb-4">
                        Attendance Records by Date
                    </h2>

                    <div id="attendance-accordion" class="space-y-4">
                        <% const dates=Object.keys(attendanceByDate); %>
                            <% if(dates.length===0) { %>
                                <p class="text-slate-500">No attendance records have been marked for this subject yet.
                                </p>
                                <% } else { %>
                                    <% dates.forEach(function(date, index) { %>
                                        <% const students=attendanceByDate[date]; %>
                                            <div class="border rounded-lg overflow-hidden shadow-sm">
                                                <button
                                                    class="date-toggle-btn w-full text-left p-4 bg-slate-50 hover:bg-slate-100 flex justify-between items-center cursor-pointer"
                                                    data-target="attendance-list-<%= index %>">
                                                    <span class="text-lg font-semibold text-indigo-700">
                                                        <%= date %>
                                                    </span>
                                                    <div class="flex items-center">
                                                        <span class="text-sm font-medium text-slate-600 mr-4">
                                                            <%= students.length %> Student<%= students.length !==1 ? 's'
                                                                    : '' %> Present
                                                        </span>
                                                        <svg class="w-5 h-5 transition-transform accordion-icon"
                                                            fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                                            <path stroke-linecap="round" stroke-linejoin="round"
                                                                stroke-width="2" d="M19 9l-7 7-7-7"></path>
                                                        </svg>
                                                    </div>
                                                </button>
                                                <div id="attendance-list-<%= index %>" class="accordion-content">
                                                    <div class="p-4 border-t border-slate-200 bg-white">
                                                        <div class="space-y-3">
                                                            <% students.forEach(function(student) { %>
                                                                <div
                                                                    class="flex justify-between items-center p-2 rounded">
                                                                    <p class="font-medium text-slate-800">
                                                                        <%= student.name %>
                                                                    </p>
                                                                    <p class="text-sm text-slate-500">
                                                                        <%= student.email %>
                                                                    </p>
                                                                </div>
                                                                <% }); %>
                                                        </div>
                                                    </div>
                                                </div>
                                            </div>
                                            <% }); %>
                                                <% } %>
                    </div>

                </div>
            </main>
        </div>
    </div>

    <script src="/javascripts/subject-attendance.js"></script>
</body>

</html>