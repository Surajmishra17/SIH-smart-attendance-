<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Teacher Dashboard</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Inter', sans-serif;
        }
    </style>
</head>

<body class="bg-slate-100 text-slate-800">

    <div class="relative min-h-screen md:flex">
        <aside id="sidebar"
            class="w-64 bg-white shadow-md flex-shrink-0 fixed z-30 inset-y-0 transform -translate-x-full md:relative md:translate-x-0 transition-transform duration-300 ease-in-out">
            <div class="p-6 text-2xl font-bold text-slate-900 border-b">
                Atten<span class="text-indigo-600">Sys</span>
            </div>
            <nav class="p-4">
                <a href="#"
                    class="flex items-center p-3 rounded-lg bg-indigo-100 text-indigo-700 font-semibold">Dashboard</a>
            </nav>
        </aside>

        <div id="menu-overlay" class="fixed inset-0 bg-black bg-opacity-50 z-20 hidden md:hidden"></div>

        <div class="flex-1 flex flex-col overflow-hidden w-full">
            <header class="bg-white border-b p-4 flex justify-between items-center">
                <button id="mobile-menu-btn" class="md:hidden p-2 rounded-md text-slate-600 hover:bg-slate-100">
                    <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor"
                        stroke-width="2">
                        <path stroke-linecap="round" stroke-linejoin="round" d="M4 6h16M4 12h16m-7 6h7" />
                    </svg>
                </button>
            
                <h1 class="text-xl font-semibold">Teacher Dashboard</h1>
            
                <div class="flex items-center space-x-4">
                    <a href="/settings" class="text-sm font-semibold text-slate-600 hover:text-indigo-600 flex items-center">
                        <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 mr-1" fill="none" viewBox="0 0 24 24"
                            stroke="currentColor" stroke-width="2">
                            <path stroke-linecap="round" stroke-linejoin="round"
                                d="M10.325 4.317c.426-1.756 2.924-1.756 3.35 0a1.724 1.724 0 002.573 1.066c1.543-.94 3.31.826 2.37 2.37a1.724 1.724 0 001.065 2.572c1.756.426 1.756 2.924 0 3.35a1.724 1.724 0 00-1.066 2.573c.94 1.543-.826 3.31-2.37 2.37a1.724 1.724 0 00-2.572 1.065c-.426 1.756-2.924 1.756-3.35 0a1.724 1.724 0 00-2.573-1.066c-1.543.94-3.31-.826-2.37-2.37a1.724 1.724 0 00-1.065-2.572c-1.756-.426-1.756-2.924 0-3.35a1.724 1.724 0 001.066-2.573c-.94-1.543.826-3.31 2.37-2.37.996.608 2.296.07 2.572-1.065z" />
                            <path stroke-linecap="round" stroke-linejoin="round" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z" />
                        </svg>
                        Settings
                    </a>
                    <a href="/logout" class="text-sm font-semibold text-slate-700 hover:text-red-500">Logout</a>
                </div>
            </header>

            <main class="flex-1 overflow-y-auto p-6">
                <div id="notification" class="hidden p-4 mb-6 text-sm rounded-lg" role="alert">
                    <span class="font-medium" id="notification-message"></span>
                </div>

                <div class="grid grid-cols-1 lg:grid-cols-3 gap-6">
                    <div class="lg:col-span-2 space-y-6">
                        <div id="class-list" class="space-y-6">
                            <% if(classes.length===0) { %>
                                <div class="bg-white p-6 rounded-lg shadow-sm">
                                    <p class="text-slate-500">No classes created yet.</p>
                                </div>
                                <% } else { %>
                                    <% classes.forEach(function(classItem) { %>
                                        <div class="bg-white p-6 rounded-lg shadow-sm">
                                            <div class="flex justify-between items-center mb-4 border-b pb-4">
                                                <div>
                                                    <h2 class="text-xl font-semibold text-slate-900">
                                                        <%= classItem.name %>
                                                    </h2>
                                                    <p class="text-sm text-slate-500">Class ID:
                                                        <code><%= classItem._id %></code></p>
                                                </div>
                                                <button
                                                    class="delete-class-btn p-2 rounded-full hover:bg-red-100 text-red-600"
                                                    data-class-id="<%= classItem._id %>" title="Delete Class">
                                                    <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5"
                                                        viewBox="0 0 20 20" fill="currentColor">
                                                        <path fill-rule="evenodd"
                                                            d="M9 2a1 1 0 00-.894.553L7.382 4H4a1 1 0 000 2v10a2 2 0 002 2h8a2 2 0 002-2V6a1 1 0 100-2h-3.382l-.724-1.447A1 1 0 0011 2H9zM7 8a1 1 0 012 0v6a1 1 0 11-2 0V8zm5-1a1 1 0 00-1 1v6a1 1 0 102 0V8a1 1 0 00-1-1z"
                                                            clip-rule="evenodd" />
                                                    </svg>
                                                </button>
                                            </div>

                                            <div class="space-y-3 mb-4">
                                                <% if(classItem.subjects.length> 0) { %>
                                                    <% classItem.subjects.forEach(function(subject) { %>
                                                        <div
                                                            class="flex flex-col sm:flex-row items-start sm:items-center justify-between p-3 bg-slate-50 rounded-lg border">
                                                            <p class="font-semibold text-slate-800 mb-2 sm:mb-0">
                                                                <%= subject.name %>
                                                            </p>
                                                            <div class="flex items-center space-x-2 w-full sm:w-auto">
                                                                <a href="/dashboard/teacher/subject/<%= subject._id %>/attendance-report"
                                                                    class="bg-blue-100 text-blue-600 px-3 py-1.5 rounded-lg text-sm font-medium hover:bg-blue-200">View</a>
                                                                <button
                                                                    class="generate-qr-btn bg-green-500 text-white px-3 py-1.5 rounded-lg text-sm font-medium hover:bg-green-600"
                                                                    data-subject-id="<%= subject._id %>">QR
                                                                    Code</button>

                                                                <button
                                                                    class="manual-attendance-btn bg-yellow-100 text-yellow-700 px-3 py-1.5 rounded-lg text-sm font-medium hover:bg-yellow-200"
                                                                    data-subject-id="<%= subject._id %>"
                                                                    data-subject-name="<%= subject.name %>">
                                                                    Manual
                                                                </button>

                                                                <button
                                                                    class="delete-subject-btn p-2 rounded-full hover:bg-red-100 text-red-500"
                                                                    data-subject-id="<%= subject._id %>"
                                                                    title="Delete Subject">&times;</button>
                                                            </div>
                                                        </div>
                                                        <% }); %>
                                                            <% } else { %>
                                                                <p class="text-slate-500 text-sm">No subjects.</p>
                                                                <% } %>
                                            </div>

                                            <form class="add-subject-form space-y-2"
                                                data-class-id="<%= classItem._id %>">
                                                <input type="text" name="subjectName"
                                                    class="w-full px-4 py-2 border border-slate-300 rounded-lg text-sm"
                                                    placeholder="New Subject Name" required>
                                                <button type="submit"
                                                    class="w-full bg-indigo-500 text-white font-semibold py-2 px-4 rounded-lg hover:bg-indigo-600 text-sm">Add
                                                    Subject</button>
                                            </form>

                                            <div class="flex justify-between items-center mt-4 border-t pt-4">
                                                <h3 class="text-sm font-medium text-slate-500">Students: <%=
                                                        (classItem.students || []).length %>
                                                </h3>
                                                <button
                                                    class="manage-students-btn text-indigo-600 hover:text-indigo-800 text-sm font-semibold"
                                                    data-students="<%= JSON.stringify(classItem.students || []) %>">Manage
                                                    Students & Device Locks</button>
                                            </div>
                                        </div>
                                        <% }); %>
                                            <% } %>
                        </div>
                    </div>

                    <div class="lg:col-span-1">
                        <div class="bg-white p-6 rounded-lg shadow-sm">
                            <h2 class="text-xl font-semibold mb-4 text-slate-900">Add Class</h2>
                            <form id="add-class-form" class="space-y-4">
                                <input type="text" name="className" class="w-full px-4 py-2 border rounded-lg"
                                    placeholder="Class Name" required>
                                <button type="submit" class="w-full bg-indigo-600 text-white py-2 rounded-lg">Create
                                    Class</button>
                            </form>
                        </div>
                    </div>
                </div>
            </main>
        </div>
    </div>

    <div id="qr-code-modal"
        class="fixed inset-0 bg-gray-800 bg-opacity-75 flex items-center justify-center hidden z-50 p-4">
        <div class="bg-white p-8 rounded-lg shadow-xl text-center w-full max-w-sm">
            <h2 class="text-2xl font-bold mb-4">Scan for Attendance</h2>
            <div id="qrcode" class="flex justify-center"></div>
            <p id="qr-timer" class="text-lg font-semibold my-4"></p>
            <button id="close-qr-modal" class="mt-4 bg-slate-500 text-white px-4 py-2 rounded-lg w-full">Close</button>
        </div>
    </div>

    <div id="student-modal"
        class="fixed inset-0 bg-gray-800 bg-opacity-75 flex items-center justify-center hidden z-50 p-4">
        <div class="bg-white p-6 rounded-lg shadow-xl w-full max-w-2xl max-h-[80vh] flex flex-col">
            <div class="flex justify-between items-center mb-4 border-b pb-2">
                <h2 class="text-xl font-bold">Manage Students</h2>
                <button id="close-student-modal" class="text-gray-500 hover:text-gray-700 text-2xl">&times;</button>
            </div>
            <div class="overflow-y-auto flex-1 pr-2">
                <div id="student-list-container" class="space-y-3"></div>
            </div>
        </div>
    </div>

    <div id="reset-confirm-modal"
        class="fixed inset-0 bg-gray-800 bg-opacity-75 flex items-center justify-center hidden z-[60] p-4">
        <div class="bg-white p-6 rounded-lg shadow-xl max-w-sm w-full text-center">
            <h3 class="text-xl font-bold text-gray-900 mb-4">Confirm Reset</h3>
            <p class="text-gray-600 mb-6">Reset device lock?</p>
            <div class="flex justify-center space-x-4">
                <button id="cancel-reset-btn" class="px-4 py-2 bg-gray-200 rounded-lg">Cancel</button>
                <button id="confirm-reset-btn" class="px-4 py-2 bg-red-600 text-white rounded-lg">Yes, Reset</button>
            </div>
        </div>
    </div>

    <div id="manual-attendance-modal"
        class="fixed inset-0 bg-gray-800 bg-opacity-75 flex items-center justify-center hidden z-50 p-4">
        <div class="bg-white p-6 rounded-lg shadow-xl w-full max-w-lg max-h-[85vh] flex flex-col">
            <div class="flex justify-between items-center mb-4 border-b pb-2">
                <h2 class="text-xl font-bold" id="manual-subject-title">Manual Attendance</h2>
                <button id="close-manual-modal" class="text-gray-500 hover:text-gray-700 text-2xl">&times;</button>
            </div>

            <div class="mb-4">
                <label class="block text-sm font-medium text-gray-700 mb-1">Select Date</label>
                <input type="date" id="manual-date-picker"
                    class="w-full border-gray-300 rounded-md shadow-sm focus:ring-indigo-500 focus:border-indigo-500 p-2 border">
            </div>

            <div class="flex-1 overflow-y-auto mb-4 border rounded-md p-2 bg-gray-50">
                <div id="manual-student-list" class="space-y-2">
                    <p class="text-center text-gray-500 py-4">Loading students...</p>
                </div>
            </div>

            <button id="save-manual-attendance"
                class="w-full bg-indigo-600 text-white font-bold py-2 rounded-lg hover:bg-indigo-700">Save
                Changes</button>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/davidshimjs-qrcodejs@0.0.2/qrcode.min.js"></script>
    <script src="/javascripts/teacher-dashboard.js"></script>
</body>

</html>