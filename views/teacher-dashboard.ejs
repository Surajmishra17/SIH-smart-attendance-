<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Teacher Dashboard</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Inter', sans-serif;
        }
    </style>
</head>

<body class="bg-slate-100 text-slate-800">

    <div class="flex h-screen">
        <aside class="w-64 bg-white shadow-md flex-shrink-0">
            <div class="p-6 text-2xl font-bold text-slate-900 border-b">
                Atten<span class="text-indigo-600">Sys</span>
            </div>
            <nav class="p-4">
                <a href="#" class="flex items-center p-3 rounded-lg bg-indigo-100 text-indigo-700 font-semibold">
                    <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6 mr-3" fill="none" viewBox="0 0 24 24"
                        stroke="currentColor" stroke-width="2">
                        <path stroke-linecap="round" stroke-linejoin="round"
                            d="M4 6a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2H6a2 2 0 01-2-2V6zM14 6a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2h-2a2 2 0 01-2-2V6zM4 16a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2H6a2 2 0 01-2-2v-2zM14 16a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2h-2a2 2 0 01-2-2v-2z" />
                    </svg>
                    Dashboard
                </a>
            </nav>
        </aside>

        <div class="flex-1 flex flex-col overflow-hidden">
            <header class="bg-white border-b p-4 flex justify-between items-center">
                <h1 class="text-xl font-semibold">Welcome, Teacher!</h1>
                <a href="/logout"
                    class="flex items-center space-x-2 rounded-lg bg-slate-100 px-4 py-2 text-sm font-semibold text-slate-700 hover:bg-slate-200 transition-colors">
                    <span>Logout</span>
                    <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24"
                        stroke="currentColor" stroke-width="2">
                        <path stroke-linecap="round" stroke-linejoin="round"
                            d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1" />
                    </svg>
                </a>
            </header>

            <main class="flex-1 overflow-y-auto p-6">
                <div id="notification" class="hidden p-4 mb-6 text-sm rounded-lg" role="alert">
                    <span class="font-medium" id="notification-message"></span>
                </div>

                <div class="grid grid-cols-1 lg:grid-cols-3 gap-6">
                    <div class="lg:col-span-2 space-y-6">
                        <div id="class-list" class="space-y-6">
                            <% if(classes.length===0) { %>
                                <div class="bg-white p-6 rounded-lg shadow-sm">
                                    <p class="text-slate-500">You haven't created any classes yet. Add one to get
                                        started!</p>
                                </div>
                                <% } else { %>
                                    <% classes.forEach(function(classItem) { %>
                                        <div class="bg-white p-6 rounded-lg shadow-sm">
                                            <div class="flex justify-between items-center mb-4 border-b pb-4">
                                                <div>
                                                    <h2 class="text-xl font-semibold text-slate-900">
                                                        <%= classItem.name %>
                                                    </h2>
                                                    <p class="text-sm text-slate-500">Class ID:
                                                        <code><%= classItem._id %></code></p>
                                                </div>
                                                <button
                                                    class="delete-class-btn p-2 rounded-full hover:bg-red-100 text-red-600"
                                                    data-class-id="<%= classItem._id %>" title="Delete Class">
                                                    <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none"
                                                        viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                                                        <path stroke-linecap="round" stroke-linejoin="round"
                                                            d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16" />
                                                    </svg>
                                                </button>
                                            </div>

                                            <div class="space-y-3 mb-4">
                                                <% if(classItem.subjects.length> 0) { %>
                                                    <% classItem.subjects.forEach(function(subject) { %>
                                                        <div
                                                            class="flex items-center justify-between p-3 bg-slate-50 rounded-lg border">
                                                            <p class="font-semibold text-slate-800">
                                                                <%= subject.name %>
                                                            </p>
                                                            <div class="flex items-center space-x-2">
                                                                <button
                                                                    class="view-attendance-btn bg-blue-100 text-blue-600 px-3 py-1.5 rounded-lg text-sm font-medium hover:bg-blue-200"
                                                                    data-subject-id="<%= subject._id %>"
                                                                    data-subject-name="<%= subject.name %>">View
                                                                    Attendance</button>
                                                                <button
                                                                    class="generate-qr-btn bg-green-500 text-white px-3 py-1.5 rounded-lg text-sm font-medium hover:bg-green-600"
                                                                    data-subject-id="<%= subject._id %>">Generate
                                                                    QR</button>
                                                                <button
                                                                    class="delete-subject-btn p-2 rounded-full hover:bg-red-100 text-red-500"
                                                                    data-subject-id="<%= subject._id %>"
                                                                    title="Delete Subject">
                                                                    <svg xmlns="http://www.w3.org/2000/svg"
                                                                        class="h-4 w-4" fill="none" viewBox="0 0 24 24"
                                                                        stroke="currentColor" stroke-width="2">
                                                                        <path stroke-linecap="round"
                                                                            stroke-linejoin="round"
                                                                            d="M6 18L18 6M6 6l12 12" />
                                                                    </svg>
                                                                </button>
                                                            </div>
                                                        </div>
                                                        <% }); %>
                                                            <% } else { %>
                                                                <p class="text-slate-500 text-sm">No subjects added to
                                                                    this class yet.</p>
                                                                <% } %>
                                            </div>

                                            <form class="add-subject-form space-y-2"
                                                data-class-id="<%= classItem._id %>">
                                                <input type="text" name="subjectName"
                                                    class="w-full px-4 py-2 border border-slate-300 rounded-lg text-sm"
                                                    placeholder="New Subject Name (e.g., Data Structures)" required>
                                                <button type="submit"
                                                    class="w-full bg-indigo-500 text-white font-semibold py-2 px-4 rounded-lg hover:bg-indigo-600 text-sm">Add
                                                    Subject</button>
                                            </form>
                                        </div>
                                        <% }); %>
                                            <% } %>
                        </div>
                    </div>

                    <div class="lg:col-span-1">
                        <div class="bg-white p-6 rounded-lg shadow-sm">
                            <h2 class="text-xl font-semibold mb-4 text-slate-900">Add a New Class</h2>
                            <form id="add-class-form" class="space-y-4">
                                <div>
                                    <label for="class-name" class="block text-sm font-medium text-slate-600 mb-1">Class
                                        Name</label>
                                    <input type="text" id="class-name" name="className"
                                        class="w-full px-4 py-2 border border-slate-300 rounded-lg"
                                        placeholder="e.g., Computer Science 2024" required>
                                </div>
                                <button type="submit"
                                    class="w-full bg-indigo-600 text-white font-semibold py-2.5 px-4 rounded-lg hover:bg-indigo-700">Create
                                    Class</button>
                            </form>
                        </div>
                    </div>
                </div>
            </main>
        </div>
    </div>

    <div id="qr-code-modal"
        class="fixed inset-0 bg-gray-800 bg-opacity-75 flex items-center justify-center hidden z-50">
        <div class="bg-white p-8 rounded-lg shadow-xl text-center">
            <h2 class="text-2xl font-bold mb-4">Scan for Attendance</h2>
            <div id="qrcode" class="flex justify-center"></div>
            <p id="qr-timer" class="text-lg font-semibold my-4"></p>
            <button id="close-qr-modal" class="mt-4 bg-slate-500 text-white px-4 py-2 rounded-lg">Close</button>
        </div>
    </div>

    <div id="attendance-modal"
        class="fixed inset-0 bg-gray-800 bg-opacity-75 flex items-center justify-center hidden z-50">
        <div class="bg-white p-8 rounded-lg shadow-xl max-w-md w-full">
            <h2 class="text-2xl font-bold mb-4" id="attendance-modal-title">Attendance</h2>
            <div id="attendance-list-container" class="space-y-2 max-h-96 overflow-y-auto">
            </div>
            <button id="close-attendance-modal"
                class="mt-6 w-full bg-slate-500 text-white px-4 py-2 rounded-lg">Close</button>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/davidshimjs-qrcodejs@0.0.2/qrcode.min.js"></script>
    <script src="/javascripts/teacher-dashboard.js"></script>
</body>

</html>